NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only. Used by the private Question Library import script; never expose in browser code.
SUPABASE_SERVICE_ROLE_KEY=

# Optional local E2E test account. Use a dedicated test host and a disposable ready quiz.
E2E_HOST_EMAIL=
E2E_HOST_PASSWORD=
E2E_QUIZ_TITLE=
